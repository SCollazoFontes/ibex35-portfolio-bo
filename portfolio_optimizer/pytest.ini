[pytest]
# Registramos el marker "network" para que pytest no lo marque como warning.
# Tests marcados con @pytest.mark.network requieren conexión a internet.
# Para saltarlos: pytest -m "not network"
markers =
    network: tests que requieren conexión a internet (yfinance, Stooq, FRED)
